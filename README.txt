Girlfriend Yes/No Page

A self-contained romantic question page.
Access token for this build: RZOX7hZpUCg23TFpTdtlg8_X

Important:
- This token is only a simple URL token, not true authentication.
- To make it genuinely private, host it behind password/authentication or an access-controlled service.
- The "No" button moves to a random position whenever she tries to use it.
- The Yes button remains in its normal position.
- Edit the questions array in index.html to customize the questions.

Local test:
Open index.html in a browser.

For a public secure-looking URL:
Upload the folder to a static hosting provider and use a long random URL path/token.
For real security, add authentication at the hosting layer.
